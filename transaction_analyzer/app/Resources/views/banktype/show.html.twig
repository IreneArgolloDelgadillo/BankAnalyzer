{% extends 'base.html.twig' %}

{% block body %}
    <h1>BankType</h1>

    <table>
        <tbody>
            <tr>
                <th>Idbanktype</th>
                <td>{{ bankType.idBankType }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ bankType.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ bankType.description }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('banktype_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('banktype_edit', { 'id': bankType.idBankType }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
