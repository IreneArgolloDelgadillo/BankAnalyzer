{% extends 'base.html.twig' %}

{% block body %}
    <h1>LoanType creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('loantype_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
