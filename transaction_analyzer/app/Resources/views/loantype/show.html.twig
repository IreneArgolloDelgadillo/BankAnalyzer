{% extends 'base.html.twig' %}

{% block body %}
    <h1>LoanType</h1>

    <table>
        <tbody>
            <tr>
                <th>Idloantype</th>
                <td>{{ loanType.idLoanType }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ loanType.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ loanType.description }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('loantype_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('loantype_edit', { 'id': loanType.idLoanType }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
